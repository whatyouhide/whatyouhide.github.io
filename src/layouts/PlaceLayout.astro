---
import BaseLayout from "./BaseLayout.astro";
import { siteConfig } from "../config";
import "../styles/fonts-playfair.css";

type Props = {
  title: string;
  subtitle?: string;
  description?: string;
  date?: Date;
  countryName: string;
  flag?: string;
  placeClass?: string;
};

const { title, subtitle, description, date, countryName, flag, placeClass } = Astro.props;

const dateFormatter = new Intl.DateTimeFormat("en-US", {
  month: "long",
  year: "numeric",
});

const metaDescription =
  description ?? `Travel notes from ${title}, ${countryName}, by ${siteConfig.fullName}.`;
---

<BaseLayout title={title} description={metaDescription}>
  <main class={`place-page${placeClass ? ` ${placeClass}` : ""}`}>
    <section class="place-hero">
      <div class="place-hero-flag">
        {flag && <span class="place-hero-flag-emoji">{flag}</span>}
        <span>{countryName}</span>
      </div>
      <h1>
        {title}
        {subtitle && <em>{subtitle}</em>}
      </h1>
      {date && <div class="place-hero-date">{dateFormatter.format(date)}</div>}
    </section>

    <div class="place-band"><div class="place-band-inner"></div></div>

    <article class="place-content">
      <slot />
    </article>
  </main>
</BaseLayout>
